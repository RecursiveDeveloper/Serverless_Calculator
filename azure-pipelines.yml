trigger:
  - main
  
pool: 
  vmImage: ubuntu-latest

resources:
  webhooks:
    - webhook: WebHook
      connection: IncomingWH

steps:  
- script: echo ${{ parameters.WebHook.resource.repository.full_name }}
